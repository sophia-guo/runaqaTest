name: "Mac, openj9, jdk11u, default build -jdk"
on:
  push:
    branches-ignore:
      - '**'
jobs:
  build:
    runs-on: macos-10.15
    steps:
    - uses: sophia-guo/build-jdk-hotspot@v1
      id: buildJDK
      with: 
        javaToBuild: 'jdk11u'
        impl: 'openj9'
    - run: ${{ steps.buildJDK.outputs.BuildJDKDir }}/bin/java -version
    - uses: actions/upload-artifact@v2-preview
      with:
        name: JDK
        path: ./**/artifacts/*.tar.gz/